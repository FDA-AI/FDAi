<?php namespace Tests\APIs;

use Illuminate\Foundation\Testing\WithoutMiddleware;
use Illuminate\Foundation\Testing\DatabaseTransactions;
use Tests\UnitTestCase;
use Tests\ApiTestTrait;
use App\Models\TrackingReminder;

class TrackingReminderApiTest extends UnitTestCase
{
    use ApiTestTrait;
    public function setUp(): void
    {
        $this->markTestSkipped('Not implemented yet.');
        parent::setUp(); // TODO: Change the autogenerated stub
    }

    public function test_create_tracking_reminder()
    {
        $trackingReminder = TrackingReminder::factory()->make()->toArray();

        $r = $this->jsonAsUser18535(
            'POST',
            '/api/v6/tracking_reminders', $trackingReminder
        );

        $this->assertApiResponse($trackingReminder);
    }

    public function test_read_tracking_reminder()
    {
        $trackingReminder = TrackingReminder::fakeSaveFromPropertyModels();

        $r = $this->jsonAsUser18535(
            'GET',
            '/api/v6/tracking_reminders/'.$trackingReminder->id
        );

        $this->assertApiResponse($trackingReminder->toArray());
    }

    public function test_update_tracking_reminder()
    {
        $trackingReminder = TrackingReminder::fakeSaveFromPropertyModels();
        $editedTrackingReminder = TrackingReminder::factory()->make()->toArray();

        $r = $this->jsonAsUser18535(
            'PUT',
            '/api/v6/tracking_reminders/'.$trackingReminder->id,
            $editedTrackingReminder
        );

        $this->assertApiResponse($editedTrackingReminder);
    }

    public function test_delete_tracking_reminder()
    {
        $trackingReminder = TrackingReminder::fakeSaveFromPropertyModels();

        $r = $this->jsonAsUser18535(
            'DELETE',
             '/api/v6/tracking_reminders/'.$trackingReminder->id
         );

        $this->assertApiSuccess();
        $r = $this->jsonAsUser18535(
            'GET',
            '/api/v6/tracking_reminders/'.$trackingReminder->id
        );

        $this->testResponse->assertStatus(404);
    }
}
